# Base URL of the deployed rail-iq-backend (FastAPI). Falls back to
# http://localhost:8000 if unset, for local dev against a locally-run backend.
VITE_API_BASE_URL=https://your-rail-iq-api.onrender.com
